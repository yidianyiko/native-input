#!/usr/bin/env bash
# Auto-activate local virtualenv + load .env when entering this directory (direnv).

if [[ -f ".env" ]]; then
  # Provided by direnv stdlib.
  dotenv
fi

if [[ -d ".venv" && -f ".venv/bin/activate" ]]; then
  source .venv/bin/activate
fi

